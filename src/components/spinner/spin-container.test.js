/* eslint-disable no-undef */
/* import React from 'react';
import { shallow } from 'enzyme';
import SpinContainer from './spin-container';

describe('<SpinContainer />', () => {
  it('should reflect SpinContainer initial state', () => {
    const wrapper = shallow(<SpinContainer fetchingData={false} />);
    const spin = wrapper.find('Spin');

    expect(spin.props().spinning).toBe(false);
    expect(spin.props().size).toBe('large');
    expect(spin.props().delay).toBe(700);
  });

  it('should render a div with Spin as data is being fetched', () => {
    const wrapper = shallow(<SpinContainer fetchingData />);
    const spin = wrapper.find('Spin');

    expect(spin.props().spinning).toBe(true);
    expect(spin.props().size).toBe('large');
    expect(spin.props().delay).toBe(700);
  });
});
*/
